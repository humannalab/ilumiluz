import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Checkout — Ilumiluz',
}

export default function CheckoutLayout({ children }: { children: React.ReactNode }) {
  return children
}
